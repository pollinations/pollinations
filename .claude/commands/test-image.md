Run tests for image.pollinations.ai service.

```bash
cd image.pollinations.ai && npm run test
```

For running specific tests:
```bash
cd image.pollinations.ai && npx vitest run --testNamePattern="$ARGUMENTS"
```
