# Weekly Update - 2025-09-13

- **Enhanced queue logging and model updates** — Implements improvements to queue management in `shared/ipQueue.js`, adding username context, user IDs, and tier information to rate limit errors and queue logs for better observability. [PR #4013](https://github.com/pollinations/pollinations/pull/4013)
- **Centralize provider data in modelCost.js** — Refactors provider information by moving it from `availableModels.js` to `modelCost.js` and updating `utils/messageSanitizer.js`, `server.js`, and `observability/tinybirdTracker.js` to utilize the centralized data. [PR #4015](https://github.com/pollinations/pollinations/pull/4015)
- **Comment out GPT-4.1 main model, keep GPT-4.1 Nano** — Temporarily disables the `openai-large` (GPT-4.1) model in the configuration while preserving the `openai-fast` (GPT-4.1 Nano) variant. [PR #4018](https://github.com/pollinations/pollinations/pull/4018)
- **Fix: Enforce max_tokens as hard cap instead of allowing user overrides** — Updates `transforms/limitChecker.js` to strictly enforce model configuration limits, ensuring user requests cannot exceed the configured `max_tokens` cap. [PR #4012](https://github.com/pollinations/pollinations/pull/4012)
- **Refactor: Transform Pipeline Modularization** — Modularizes the text generation pipeline by creating dedicated transform modules for Portkey headers, request limit validation, message sanitization, and parameter processing to reduce code duplication. [PR #3977](https://github.com/pollinations/pollinations/pull/3977)
- **Add CapyUniverse - CapyIMG to creative projects** — Adds CapyIMG, a browser-based AI image generation tool using the Pollinations API, to the Creative Projects category. [PR #4008](https://github.com/pollinations/pollinations/pull/4008)
- **Pollen MVP** — Introduces the MVP for Pollen, including a new auth backend using `better-auth`, `polar.sh` integration, and `drizzle` for database management. [PR #3676](https://github.com/pollinations/pollinations/pull/3676)
