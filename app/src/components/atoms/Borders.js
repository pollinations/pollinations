import styled from '@emotion/styled'

export const HorizontalBorder = styled.div`

height: ${props => props.Height ? props.Height : '1px'};
margin-top: ${props => props.margin ? props.margin : '1em 0 0 0'};
background-color: white;
`