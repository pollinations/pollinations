{
  "target": "worker",
  "credentials": "myceli",
  "install": "npm ci && npm ci --prefix ../.. --workspaces=false",
  "build": "npm run test",
  "deploy": "npm run deploy",
  "watch": [
    "packages/mcp/"
  ],
  "verify": [
    "https://ask-jev-mcp.pollinations.ai/health"
  ]
}
