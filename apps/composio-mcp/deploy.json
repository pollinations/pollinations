{
  "target": "worker",
  "credentials": "myceli",
  "install": "npm ci",
  "build": "npm test",
  "deploy": "npm run deploy",
  "watch": [
    "shared/mcp-usage.ts",
    "shared/registry/mcp.ts"
  ]
}
