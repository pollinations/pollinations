{
  "target": "worker",
  "credentials": "myceli",
  "sops": "none",
  "docker": true,
  "install": "npm ci",
  "build": "npm test",
  "deploy": "npm run deploy",
  "watch": [
    "shared/registry/mcp.ts"
  ]
}
