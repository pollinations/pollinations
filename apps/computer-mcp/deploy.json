{
  "target": "worker",
  "credentials": "myceli",
  "docker": true,
  "sops": "none",
  "install": "npm ci",
  "build": "npm test",
  "deploy": "npm run deploy",
  "watch": [
    "shared/registry/mcp.ts"
  ]
}
