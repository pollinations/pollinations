{
  "target": "worker",
  "credentials": "myceli",
  "subdomain": "mcp",
  "install": "npm ci && npm ci --prefix ../../packages/mcp",
  "build": "npm run test",
  "deploy": "npm run deploy -- --env staging && npm run deploy",
  "watch": ["packages/mcp/"]
}
