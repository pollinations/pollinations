{
  "name": "pollinations-mcp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker.test.js",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "wrangler": "^4.122.0"
  }
}
