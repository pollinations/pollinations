apiVersion: 1

policies:
  - orgId: 1
    receiver: Discord Critical
    group_by:
      - alertname
      - model
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: Discord Critical
        matchers:
          - severity = critical
        continue: false
