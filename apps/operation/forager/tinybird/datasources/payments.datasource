TOKEN "treasury_ingest" APPEND
TOKEN "treasury_ingest" READ

SCHEMA >
    `paid_at` Date `json:$.paid_at`,
    `month` String `json:$.month`,
    `provider` String `json:$.provider`,
    `counterparty` String `json:$.counterparty`,
    `amount_eur` Float64 `json:$.amount_eur`,
    `amount_usd` Float64 `json:$.amount_usd`,
    `wise_ref` String `json:$.wise_ref`,
    `pulled_at` Date `json:$.pulled_at`

ENGINE "MergeTree"
ENGINE_SORTING_KEY "month, provider, paid_at"
