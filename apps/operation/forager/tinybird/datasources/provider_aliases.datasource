TOKEN "treasury_ingest" APPEND
TOKEN "treasury_ingest" READ

SCHEMA >
    `entered_at` DateTime `json:$.entered_at`,
    `alias` String `json:$.alias`,
    `provider` String `json:$.provider`

ENGINE "MergeTree"
ENGINE_SORTING_KEY "alias, entered_at"
