TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        month,
        source,
        vendor,
        currency,
        credit,
        paid
    FROM meter_monthly
    ORDER BY vendor, month, source, currency

TYPE endpoint
