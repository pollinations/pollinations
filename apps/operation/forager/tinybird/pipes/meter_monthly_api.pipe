TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        month,
        source,
        provider,
        funding,
        cost_usd
    FROM meter_monthly
    ORDER BY provider, month, funding, source

TYPE endpoint
