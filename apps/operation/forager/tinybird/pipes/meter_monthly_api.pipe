TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        month,
        source,
        provider,
        funding,
        currency,
        amount
    FROM meter_monthly
    ORDER BY provider, month, funding, source

TYPE endpoint
