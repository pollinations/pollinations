TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        month,
        source,
        provider,
        currency,
        credit,
        paid
    FROM meter_monthly
    ORDER BY provider, month, source, currency

TYPE endpoint
