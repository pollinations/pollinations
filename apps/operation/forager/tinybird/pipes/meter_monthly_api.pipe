TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        month,
        source,
        provider,
        currency,
        credit_amount,
        cash_amount
    FROM meter_monthly
    ORDER BY provider, month, source, currency

TYPE endpoint
