TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        provider,
        funding,
        month,
        source,
        cost_usd
    FROM meter_monthly
    ORDER BY provider, month, funding, source

TYPE endpoint
