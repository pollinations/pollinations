TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        scope,
        key,
        field,
        formatDateTime(entered_at, '%F %T') AS entered_at,
        value_num,
        value_str,
        note
    FROM overrides
    ORDER BY entered_at DESC, scope, key, field

TYPE endpoint
