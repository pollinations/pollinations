TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        provider,
        category,
        formatDateTime(paid_at, '%F') AS paid_at,
        counterparty,
        amount_eur
    FROM payments
    ORDER BY paid_at DESC, counterparty

TYPE endpoint
