TOKEN "treasury_web" READ

NODE provider_aliases_latest
SQL >
    SELECT alias,
           argMax(provider, entered_at) AS provider
    FROM provider_aliases
    GROUP BY alias
    HAVING provider != ''
    ORDER BY alias

TYPE endpoint
