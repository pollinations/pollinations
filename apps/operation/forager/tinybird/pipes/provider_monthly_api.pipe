TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        month,
        source,
        vendor,
        currency,
        category,
        credit,
        paid
    FROM provider_monthly
    ORDER BY vendor, month, source, currency, category

TYPE endpoint
