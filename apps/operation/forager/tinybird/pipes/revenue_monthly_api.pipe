TOKEN "treasury_web" READ

NODE revenue
SQL >
    SELECT
        month,
        'stripe' AS source,
        currency,
        gross_amount,
        fees_amount,
        refunds_amount
    FROM revenue_monthly ORDER BY month, currency

TYPE endpoint
