TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        formatDateTime(date, '%F') AS date,
        vendor,
        category,
        charged_amount,
        charged_currency,
        paid_amount,
        paid_currency,
        invoice_ref,
        match_status
    FROM transactions
    ORDER BY date DESC, vendor, category, match_status

TYPE endpoint
