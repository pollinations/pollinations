TOKEN "treasury_web" READ

NODE endpoint
SQL >
    SELECT
        formatDateTime(date, '%F') AS date,
        vendor,
        category,
        charged_amount,
        charged_currency
    FROM transactions
    ORDER BY date DESC, vendor, category

TYPE endpoint
