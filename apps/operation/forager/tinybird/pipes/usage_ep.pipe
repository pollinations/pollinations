TOKEN "treasury_web" READ

NODE usage
SQL >
    SELECT
        month,
        provider,
        model,
        cost_paid_pollen,
        cost_quest_pollen,
        billable_paid_pollen,
        billable_quest_pollen
    FROM usage_monthly
    ORDER BY month, provider, model

TYPE endpoint
