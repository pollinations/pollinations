TOKEN "treasury_web" READ

NODE usage
SQL >
    SELECT
        month,
        'tinybird' AS source,
        provider,
        model,
        currency,
        cost_paid,
        cost_quests,
        price_paid,
        price_quests
    FROM usage_monthly
    ORDER BY month, provider, model

TYPE endpoint
