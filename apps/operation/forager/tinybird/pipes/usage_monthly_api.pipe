TOKEN "treasury_web" READ

NODE usage
SQL >
    SELECT
        month,
        'tinybird' AS source,
        provider,
        model,
        cost_paid_pollen,
        cost_quest_pollen,
        billable_paid_pollen,
        billable_quest_pollen
    FROM usage_monthly
    ORDER BY month, provider, model

TYPE endpoint
