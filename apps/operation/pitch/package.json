{
  "name": "myceli-pitch",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export": "slidev export",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=myceli-pitch"
  },
  "dependencies": {
    "@slidev/cli": "^52.13.0",
    "@slidev/theme-default": "^0.25.0"
  }
}
