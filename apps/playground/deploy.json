{
  "target": "pages",
  "credentials": "apps",
  "subdomain": "playground",
  "install": "npm install --silent && npm install --silent --prefix ../..",
  "build": "npm run build",
  "output": "dist",
  "watch": [
    "packages/sdk/",
    "packages/ui/",
    "package.json",
    "package-lock.json"
  ]
}
