[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "polli-core"
version = "0.1.0"
description = "High-performance Rust core for Polli Discord bot"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "polli_core"
