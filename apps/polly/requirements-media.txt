# Optional dependencies for media handlers in Polly

# Table rendering with PIL and emoji support
pillow>=10.0.0
pilmoji>=2.0.0

# LaTeX rendering (SVG to PNG conversion)
cairosvg>=2.7.0

# For LaTeX API requests
requests>=2.31.0
