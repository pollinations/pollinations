diff --git a/node_modules/better-auth/dist/plugins/api-key/routes/verify-api-key.mjs b/node_modules/better-auth/dist/plugins/api-key/routes/verify-api-key.mjs
index c18cb8a..a91927e 100644
--- a/node_modules/better-auth/dist/plugins/api-key/routes/verify-api-key.mjs
+++ b/node_modules/better-auth/dist/plugins/api-key/routes/verify-api-key.mjs
@@ -111,13 +111,17 @@ async function validateApiKey({ hashedKey, ctx, opts, schema, permissions }) {
 		code: "RATE_LIMITED",
 		details: { tryAgainIn }
 	});
-	const updated = {
-		...apiKey,
+	// PATCHED: Only update rate-limit fields, not permissions
+	const rateUpdate = {
 		...update,
 		remaining,
 		lastRefillAt,
 		updatedAt: /* @__PURE__ */ new Date()
 	};
+	const updated = {
+		...apiKey,
+		...rateUpdate
+	};
 	const performUpdate = async () => {
 		if (opts.storage === "database") return ctx.context.adapter.update({
 			model: API_KEY_TABLE_NAME,
@@ -125,10 +129,7 @@ async function validateApiKey({ hashedKey, ctx, opts, schema, permissions }) {
 				field: "id",
 				value: apiKey.id
 			}],
-			update: {
-				...updated,
-				id: void 0
-			}
+			update: rateUpdate
 		});
 		else if (opts.storage === "secondary-storage" && opts.fallbackToDatabase) {
 			const dbUpdated = await ctx.context.adapter.update({
@@ -137,10 +138,7 @@ async function validateApiKey({ hashedKey, ctx, opts, schema, permissions }) {
 					field: "id",
 					value: apiKey.id
 				}],
-				update: {
-					...updated,
-					id: void 0
-				}
+				update: rateUpdate
 			});
 			if (dbUpdated) await setApiKey(ctx, dbUpdated, opts);
 			return dbUpdated;
