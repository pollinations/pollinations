/**
 * Shared constants used across the client application
 */

export { PUBLISHABLE_KEY_PREFIX, SECRET_KEY_PREFIX } from "../constants.ts";

/** LocalStorage key for storing the redirect URL during authentication flow */
export const REDIRECT_URL_STORAGE_KEY = "pollinations_redirect_url";
