/**
 * Shared constants used across the application (both client and server)
 */

/** API key prefixes used throughout the application */
export const PUBLISHABLE_KEY_PREFIX = "plln_pk";
export const SECRET_KEY_PREFIX = "plln_sk";
