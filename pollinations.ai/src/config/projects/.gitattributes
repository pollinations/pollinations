# Auto-resolve merge conflicts for project files
# Since we only append entries, the union strategy keeps all lines from both branches
# This prevents merge conflicts when multiple app submission PRs are open in parallel

chat.js merge=union
creative.js merge=union
games.js merge=union
hackAndBuild.js merge=union
learn.js merge=union
socialBots.js merge=union
vibeCoding.js merge=union
