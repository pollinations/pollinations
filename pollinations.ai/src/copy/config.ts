/**
 * Copy Processing Configuration
 * Centralized config for translation and transformation
 */

export const COPY_CONFIG = {
    // Model to use for processing
    model: "gemini",

    // Maximum variation seed (1 to maxSeed)
    maxSeed: 5,
};
