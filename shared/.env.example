# Legacy tokens for authentication (comma-separated list)
# These tokens will bypass the queue when provided in requests
LEGACY_TOKENS=foo123,bar456

# Allowlisted domains for queue bypass (comma-separated list)
# Requests from these domains will bypass the queue
ALLOWLISTED_DOMAINS=pollinations.ai,roblox.com

# Queue interval for text service (in milliseconds)
# Controls how frequently requests from the same IP are processed
QUEUE_INTERVAL_MS_TEXT=6000

# Queue interval for image service (in milliseconds)
QUEUE_INTERVAL_MS_IMAGE=10000

# Note: The shared utilities automatically load this file
# Services only need to import the utilities - no additional setup required
