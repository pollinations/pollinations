{
    "name": "@pollinations/icons",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "description": "Generates each app's web identity (favicons, app icons, OG, manifest) from the @pollinations_ai/ui logo + theme colors.",
    "bin": {
        "pollinations-icons": "src/cli.js"
    },
    "scripts": {
        "generate": "node src/cli.js",
        "test": "node --test"
    },
    "dependencies": {
        "@pollinations_ai/ui": "*",
        "sharp": "^0.33.5"
    }
}
